const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../layout';

describe('core.actions.layout', () => {
  it('should do something');
});
