const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../logout';

describe('account.actions.logout', () => {
  it('should do something');
});
